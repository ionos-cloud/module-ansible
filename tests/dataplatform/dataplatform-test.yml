- hosts: localhost
  connection: local
  gather_facts: false

  vars_files:
  - ./vars.yml

  tasks:

    - name: Delete Data Platform cluster
      dataplatform_cluster:
        cluster: "AnsibleAutoTestDataPlatform"
        state: absent
        wait: true
